package com.aura.core.designsystem.theme

import androidx.compose.ui.graphics.Color

// ============================================================================
// AURA BRAND
// ============================================================================

/**
 * AURA's signature brand red.
 *
 * Use selectively:
 * - active navigation
 * - selected playback controls
 * - current track
 * - favorite state
 * - primary actions
 * - small brand accents
 *
 * Do not flood entire screens with this color.
 */
val AuraRed = Color(0xFFE23D36)

val AuraRedPressed = Color(0xFFC8312C)

val AuraRedDeep = Color(0xFF9F2724)

val AuraRedSoft = Color(0xFFF15A53)


// ============================================================================
// OBSIDIAN
// ============================================================================

/**
 * Primary AURA appearance.
 *
 * Deep charcoal rather than pure black so surfaces can still have
 * meaningful depth and separation.
 */
val ObsidianBackground = Color(0xFF070707)

val ObsidianSurface = Color(0xFF0E0E0E)

val ObsidianSurfaceElevated = Color(0xFF161616)

val ObsidianSurfaceHighest = Color(0xFF1D1D1D)

val ObsidianBorder = Color(0xFF292929)

val ObsidianBorderSoft = Color(0xFF202020)

val ObsidianTextPrimary = Color(0xFFF3F0EC)

val ObsidianTextSecondary = Color(0xFFAAA6A1)

val ObsidianTextMuted = Color(0xFF716E69)

val ObsidianIconPrimary = Color(0xFFE9E6E1)

val ObsidianIconSecondary = Color(0xFF99958F)

val ObsidianIconDisabled = Color(0xFF56534F)


// ============================================================================
// AMOLED
// ============================================================================

/**
 * True-black appearance for OLED displays.
 */
val AmoledBackground = Color(0xFF000000)

val AmoledSurface = Color(0xFF050505)

val AmoledSurfaceElevated = Color(0xFF0D0D0D)

val AmoledSurfaceHighest = Color(0xFF151515)

val AmoledBorder = Color(0xFF222222)

val AmoledTextPrimary = Color(0xFFF6F6F6)

val AmoledTextSecondary = Color(0xFFAAAAAA)

val AmoledTextMuted = Color(0xFF707070)

val AmoledIconPrimary = Color(0xFFEEEEEE)

val AmoledIconSecondary = Color(0xFFA0A0A0)

val AmoledIconDisabled = Color(0xFF555555)


// ============================================================================
// IVORY
// ============================================================================

/**
 * Warm editorial light appearance.
 *
 * Avoid sterile pure-white backgrounds throughout the entire UI.
 */
val IvoryBackground = Color(0xFFF4F0E8)

val IvorySurface = Color(0xFFFBF8F2)

val IvorySurfaceElevated = Color(0xFFFFFFFF)

val IvorySurfaceHighest = Color(0xFFFFFFFF)

val IvoryBorder = Color(0xFFD8D2C9)

val IvoryBorderSoft = Color(0xFFE5DFD6)

val IvoryTextPrimary = Color(0xFF191816)

val IvoryTextSecondary = Color(0xFF625E58)

val IvoryTextMuted = Color(0xFF8A847C)

val IvoryIconPrimary = Color(0xFF24211E)

val IvoryIconSecondary = Color(0xFF756F68)

val IvoryIconDisabled = Color(0xFFAAA49C)

/**
 * Slightly deeper than the dark-theme red to maintain strong contrast
 * against warm Ivory surfaces.
 */
val IvoryAccent = Color(0xFFB82F2A)

val IvoryAccentPressed = Color(0xFF992621)


// ============================================================================
// SEMANTIC COLORS
// ============================================================================

val AuraErrorDark = Color(0xFFFF6B64)

val AuraErrorLight = Color(0xFFB3261E)

val AuraSuccessDark = Color(0xFF77B784)

val AuraSuccessLight = Color(0xFF356A40)

val AuraWarningDark = Color(0xFFE1A552)

val AuraWarningLight = Color(0xFF8A5A13)